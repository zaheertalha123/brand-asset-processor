Pillow>=10.0.0

# EXIF metadata for JPEG / ownership tags (add_company_metadata.py)
piexif>=1.1.3

# Bundles an FFmpeg binary used to watermark videos and strip audio.
# Alternative: install system FFmpeg and ensure `ffmpeg` is on PATH.
#   Windows: https://ffmpeg.org/download.html  or  winget install FFmpeg
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
# Also used by seo_enhancements.py for MP4 (H.264 + AAC) conversion.
imageio-ffmpeg>=0.5.0
